import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  height: 100vh;
  width: 100%;
  background-color: black;
`;

const ResumeSection = () => {
  return <Wrapper>ResumeSection</Wrapper>;
};

export default ResumeSection;
